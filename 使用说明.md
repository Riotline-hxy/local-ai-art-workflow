# GPT Image Playground 本地使用说明

安装目录：C:\Users\11768\Documents\GTIIT learning\Agent\photo
项目来源：https://github.com/alasano/gpt-image-playground

## 首次配置

用文本编辑器打开本目录的 `.env.local`，将 `OPENAI_API_KEY` 后面的占位值换成你自己的 API Key。不要把密钥发到聊天中。保存后启动或重启服务。

- `OPENAI_API_KEY`：必填，填入你使用的图片 API 服务的密钥。
- `OPENAI_API_BASE_URL`：可选。使用官方接口时保留注释；使用兼容服务时取消注释并填写服务商提供的完整 base URL，通常以 `/v1` 结尾。不要填聊天网页地址或完整的 `/images/generations` 路径。服务需支持项目使用的 Images API，只有聊天接口兼容并不够。
- `NEXT_PUBLIC_IMAGE_STORAGE_MODE=fs`：已配置为本地磁盘存图，图片保存在 `generated-images` 文件夹。
- `APP_PASSWORD`：可选，取消注释并设置后，页面提交请求时会要求密码。

`.env.local` 已被项目的 `.gitignore` 忽略。当前 API Key 是占位值，需要替换后才能生成图片。模型是否可用取决于你所用服务和账号的权限、额度及验证要求。

## 启动与停止

双击 `start-photo.cmd`，等终端显示 Ready 后，在浏览器打开 http://localhost:3000 。启动脚本只监听本机回环地址。保持终端窗口运行；停止时按 Ctrl+C。

也可以在 PowerShell 中执行：

```powershell
cd 'C:\Users\11768\Documents\GTIIT learning\Agent\photo'
npm.cmd run dev -- --hostname 127.0.0.1 --port 3000
```

如果 3000 端口被其他程序占用，把命令中的 3000 改为 3001，然后打开 http://localhost:3001 。浏览器历史按地址保存，建议以后固定使用同一个地址和端口。

## 生成图片

1. 打开 Generate 模式。
2. 在 Model 中选择你的服务支持的模型。当前仓库提供 gpt-image-2、gpt-image-1.5、gpt-image-1、gpt-image-1-mini 选项；下拉列表存在不代表账号一定有权限。
3. 输入提示词，例如：一只戴着绿色帽子的鹈鹕骑自行车经过海岸，二维绘本插画，柔和的薄荷绿和米黄色，横向构图。
4. 第一次建议生成 1 张，选择标准尺寸和较低质量，确认服务可用后再提高质量、尺寸或数量。
5. 点击 Generate，等待结果，使用结果区域的下载按钮保存。

## 编辑图片

切换 Edit，上传或粘贴原图，输入修改要求。需要局部修改时使用内置蒙版工具绘制修改区域，或上传蒙版，再提交编辑。蒙版效果取决于模型，不能保证区域外完全不变。也可以用结果或历史中的 Send to Edit 继续修改。

## 图片、历史与费用

本地图片：本目录的 `generated-images`。
历史元数据：当前浏览器的 localStorage；清除网站数据、换浏览器、换地址或端口后，原历史不一定出现。建议另行备份重要图片。
项目显示的费用是代码计算的估算值；实际额度和费用以所用 API 服务账单为准。

## 常见问题

- 401 / invalid_api_key：检查密钥及 base URL 是否属于同一个服务，并重启。
- model_not_found / permission / 403：改选账号可用模型，并检查服务权限或验证要求。
- 429 / insufficient_quota：检查额度、余额和请求频率。
- 连接超时：检查网络、服务地址及本机代理设置。
- 首次打开编译较慢：开发模式会首次编译页面；项目使用 Google Fonts，构建时也可能需要访问字体服务。
- 修改环境变量后无效：停止进程后重新启动。

## 更新与生产运行

更新前备份 `.env.local` 和 `generated-images`，然后执行 `git pull --ff-only` 和 `npm.cmd ci`。本地附加的启动脚本及本说明不属于上游代码。

如需生产模式，在配置好 `.env.local` 后运行：

```powershell
npm.cmd run build
npm.cmd run start -- --hostname 127.0.0.1 --port 3000
```

## 本次安装记录

已使用 Node.js 24.16.0 安装依赖。为修复依赖审计中报告的问题，已更新 package-lock.json，并将 package.json 中 PostCSS 和 brace-expansion 的旧补丁锁定值更新为 8.5.28 和 1.1.18，增加 Vite 6.4.3 补丁锁定。Next.js 实际安装为 16.3.5。本次 npm audit 检查为 0 个已知漏洞。

更新上游前，`git status` 会显示这些本地依赖调整。保留这些更改；如上游也修改依赖文件，应先合并差异再重新安装，不要直接丢弃修复。

当前密钥仍为占位值，尚未调用真实 API 验证生图和编辑。
